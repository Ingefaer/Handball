package com.example.entities;

import com.example.data.DataLayer;

public class TestClass {
    public static void main(String[] args) {
        DataLayer dataLayer = new DataLayer();

    //Timer timer = new Timer();
    //timer.setTotalSeconds(127);
    //Timestamp timestamp1 = new Timestamp(timer);


        //System.out.println(timer);
        //System.out.println(timestamp1);

        //dataLayer.getLeague();

        //dataLayer.getAllMatches();
        //dataLayer.getGoals(2,5);

        /*

        FXMLLoader loader = new FXMLLoader(App.class.getResource("report.fxml"));
            Parent root = loader.load();

            ReportController reportController = loader.getController();
            reportController.importSelectedMatch(match);
            reportController.setButtonsVisible();

            App.setRoot(root);

        @FXML
        private void switchToMatch() throws IOException {
        App.setRoot("match");
        }

        menuMatchButton.setVisible(false);
        matchMatchButton.setVisible(false);

        public void setButtonsVisible(){
        menuMatchButton.setVisible(true);
        matchMatchButton.setVisible(true);
        }

         */
    }
}
